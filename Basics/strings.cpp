#include <bits/stdc++.h>
using namespace std;

int main() {
    /*
    string s;
    cin >> s; //input: hello world
    cout << s; //output: hello

    string s2;
    cin>> s2;
    cout << s << s2; //outputs: helloveda
    */

   /*
   string str;
   getline(cin, str); //picksup only till the line break, won't take the next line
   cout << str; //outputs: hello veda
   */

  string s1 = "Vedanshee";
  int len = s1.size();
  cout << s1[len]; // NULL
  cout << "\n" << s1[len-1];
   
    return 0;
}
